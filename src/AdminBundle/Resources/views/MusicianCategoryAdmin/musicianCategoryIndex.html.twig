{% extends "::base-admin.html.twig" %}

{% block content %}

    <p>Voici toutes les styles de musicien enregistrées dans notre BDD : </p>
    <br>
    <a class="btn btn_add_musicianCategory" href="{{ path("addMusicianCategory") }}">Ajouter style de musicien</a>

    <table id="table_admin" class="table table-striped text-center display">
        <thead>
        <tr class="listTd">
            <th>Id</th>
            <th>Nom</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for musicianCategory in musicianCategories %}
            <tr>
                <td>{{ musicianCategory.id }}</td>
                <td>{{ musicianCategory.label }}</td>
                <td>
                    <div class="btn-group">
                        <button type="button" class="btn btn_action btn-sm dropdown-toggle" data-toggle="dropdown"
                                aria-haspopup="true" aria-expanded="false">
                            Action <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu ">
                            <li><a href="{{ path("editMusicianCategory", {"id": musicianCategory.id}) }}"><i
                                            class="glyphicon glyphicon-pencil"></i> &nbsp;Editer</a></li>
                            <li><a href="{{ path("deleteMusicianCategory", {"id": musicianCategory.id}) }}"><i
                                            class="glyphicon glyphicon-remove"></i> &nbsp;Supprimer</a></li>

                        </ul>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}