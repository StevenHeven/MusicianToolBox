{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% extends "::base-admin.html.twig" %}

{% block content %}

    <p> Modifier le concert  :</p>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('concert.index') }}" class="btn btn-back"><span class="glyphicon glyphicon-chevron-left"></span> Annuler</a>

{% endblock %}